# Repository agent instructions

- 使用 PowerShell 读取或写入文件时必须显式注意编码。仓库文件默认按 UTF-8 处理；尽量使用 `apply_patch`、Node.js 或其他能可靠保持 UTF-8 的工具编辑文件，避免用 PowerShell 直接写文件。
- 完成用户要求的代码修改并通过合理验证后，默认创建提交、合并到 `main`，并推送到 `origin/main`。除非用户明确要求不要提交、不要合并或不要推送，否则不要把已完成的工作仅留在工作区或功能分支。
- 合并主线前保护与当前任务无关的用户修改；如存在冲突、权限不足、分支保护或验证失败，应明确报告阻塞，不得丢弃或覆盖用户工作。
